<div class="client-card-container">
  <%= link_to '', user_path(client), class: 'cover-link' %>
  <div class="client-info">
    <h2><%= client.first_name %> <%= client.last_name %></h2>
    <h3><%= client.address_street %> <%= client.address_zip %>, <%= client.address_city.upcase %></h3>
    <h4>Dernière notification : <%= client.updated_at.strftime('%d/%m/%Y') %></h4>
  </div>
  <%= link_to 'Call', "tel:#{client.phone_number}", class: "icon-call" %>
</div>





