<div class="job-card-container completed">
  <%= link_to '', job_path(job), class: 'cover-link' %>
  <div class="job-info">
    <h2><%= job.name %></h2>
    <h4>Terminée le <%= job.updated_at.strftime('%d/%m/%Y') %></h4>
  </div>
  <i class="fas fa-check"></i>

</div>
