<%= render partial: "shared/navbar", locals: {
  title: "Bonjour #{@jobs.first.user.first_name} !",
  sub_title: "#{@jobs.count} tâches • Dernier envoi : #{@jobs.last.created_at.strftime('%d/%m/%Y')}", # a definir!
  button: ""
  } %>

<%= link_to 'voir les documents', documents_path %>

<% @jobs.each do |job| %>
      <%= render partial: "shared/task_card", locals: {job: job} %>
<% end %>



