

<%= render partial: "shared/navbar", locals: {
  title: "Nouvelle tâche",
  sub_title: "pour #{@job.user.first_name} #{@job.user.last_name}",
  button: job_path(@job),
  user: @job.user
  } %>

<div class="centerflextask">
  <div class="field">
  <%= simple_form_for [@job, @task] do |f| %>
  <%= f.input :title, label: 'Titre', hint:"Essayer de garder votre titre concis"%>
  <%= f.input :content, label: 'Description' %>
  <%= f.file_field :photos, label: false, as: :file, multiple: true %>
  <%= f.submit "Envoyer"%>
  <% end %>
  </div>
</div>
